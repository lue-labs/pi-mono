<task> Adversarial architecture review of ADR 0010. Read the following files first, then produce findings: REQUIRED READS (in order): 1. /Users/luke/Projects/personal/my-pi/docs/adr/0010-memory-scope-enforced-by-physical-partition.md (the ADR under review) 2. /Users/luke/Projects/personal/my-pi/docs/adr/0007-unified-cross-harness-memory-store.md 3. /Users/luke/Projects/personal/my-pi/docs/adr/0008-memory-cache-stability-contract.md 4. /Users/luke/Projects/personal/my-pi/docs/adr/0009-memory-reconciliation-contract.md 5. /Users/luke/Projects/personal/my-pi/extensions/dream-memory/src/canonical/paths.ts 6. /Users/luke/Projects/personal/my-pi/extensions/dream-memory/src/canonical/frontmatter.ts 7. /Users/luke/Projects/personal/my-pi/extensions/dream-memory/src/store.ts (focus: searchMemoryHttp, collections param, no index param) 8. /Users/luke/Projects/personal/my-pi/extensions/dream-memory/src/active-memory/recall-routing.ts (focus: activeRecallCollections) 9. /Users/luke/Projects/personal/my-pi/extensions/dream-memory/docs/qmd-collections.md (NOTE: layout described here is STALE — paths.ts is authoritative) PREMISE UNDER ATTACK: ADR 0010 claims: qmds ONLY query-time filter is -c collection (verified via qmd query --help: grammar = lex/vec/hyde only; HTTP /query body = {searches, collections, limit, rerank}). Therefore scope/visibility ACL MUST be enforced by physical partition = collection membership, not frontmatter flags. Frontmatter trimmed: scope dropped (derived from path), visibility dropped, sensitivity collapsed to optional secret, origin folded into provenance, device_id added. Read-set per session: canonical-global (always) + canonical-project-{slug}. Secrets → separate qmd --index. GC reindex is eventual via next dream.

ADVERSARIAL ANGLES — pursue ALL of these and any others you find:

1. IS THE CORE PREMISE TRUE? Did we miss a qmd capability (metadata filter, per-path scoping, field filter on query docs) that makes a less-invasive design viable? Is qmd query --help sufficient evidence, or must we check qmd docs/SYNTAX.md or the daemon /query API spec directly?

2. WRITE-SIDE CORRECTNESS HOLE: removing scope frontmatter means the directory IS the scope with no independent backstop. paths.ts explicitly kept scope so a misfiled note stays classifiable. If a writer puts a file in the wrong directory, there is now no way to detect it. Real risk? Acceptable loss?

3. NO-GIT-ROOT SESSIONS: read-set = [canonical-global, canonical-project-{sanitize(gitRoot)}]. What happens when session has no git root (running in ~, non-repo dir)? Fail-closed (only global)? Fail-open? Crash? Is project isolation actually guaranteed?

4. MID-SESSION REGISTRATION RACE vs ADR 0008 SPAN-STABILITY: read-set is resolved once at session start and span-stable. But collections auto-register on first canonical write, which may happen mid-session. Memory written this session is not in the read-set until next session (not recallable same-session). Acceptable? Does registering a collection mid-session perturb anything cached?

5. SECRET SEPARATE-INDEX FEASIBILITY: ADR asserts secrets go in a separate qmd --index. Was it verified that the qmd daemon (HTTP mode) can target/serve multiple named indexes concurrently, and that the harness can address them? This is committing to plumbing that may not be buildable.

6. GC EVENTUAL-REINDEX RACE: between GC file-delete and next dreams qmd update, recall can return a hit whose file is gone (qmd get → 404). ADR says recall must tolerate a missing doc. Does the always-loaded Tier-1 MEMORY.md snapshot (ADR 0009) also reference deleted files until re-rendered? Is degrade/skip sufficient or is there a stale-pointer hole? 7. MIGRATION RIPPLE REALISM: reconcile/cluster code reads .scope (~29 refs). ADR claims scope moves to a sidecar column populated from path. Is that path real or does dropping the frontmatter field break the reconcile input contract? Trace actual code. 8. FEDERATION COST: store.ts fans out across collections (Promise.all(collections.flatMap(...))). Two collections is cheap, but does the per-session read-set ever grow (multiple projects, secret-index opt-in) in a way that hurts hot recall latency budget? GROUNDING RULES: - Every finding MUST cite specific file:line or a concrete quote from one of the files you read. - If a file is missing or unreadable, say so explicitly — do not guess its contents. - If qmd behavior is inferred rather than observed in the codebase, label the finding as UNVERIFIED ASSUMPTION and flag what to check. - Do not manufacture problems. If an angle is genuinely sound, say SOUND and explain why. OUTPUT CONTRACT: Return a flat numbered list. Each item: SEVERITY: BLOCKER | CONCERN | NIT | SOUND | UNVERIFIED ASSUMPTION TARGET: which claim, section, or angle FLAW: the specific problem (one tight paragraph) FIX/VERIFY: what to do or check End with a one-line verdict: SHIP / SHIP-WITH-FIXES / RECONSIDER Do NOT edit any files. This is read-only adversarial review. </task> <default_follow_through_policy> Read all listed files before forming any judgment. Do not skip files. If a file is absent, note it in findings but continue with the rest. Complete all 8 angles plus any extras found. Do not ask clarifying questions — just produce findings. </default_follow_through_policy> <grounding_rules> All claims must cite file:line or a direct quote. Inferences about external tool behavior (qmd) must be labeled UNVERIFIED ASSUMPTION. Do not invent problems; if something is sound, say so. </grounding_rules> <action_safety> Read-only. Do not write, edit, or create any files. Return findings only. </action_safety>
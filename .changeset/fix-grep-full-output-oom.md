---
"@valkyriweb/pi-coding-agent": patch
---

Bound Grep's formatted model-facing output before joining lines so broad or `full:true` searches cannot crash the Pi process with an out-of-memory error.

Suppress skill collision diagnostics for byte-identical duplicate skill installs while still warning on divergent same-name skills.

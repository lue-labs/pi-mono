---
"@lue-labs/pi-ai": minor
"@lue-labs/pi-coding-agent": patch
---

Honor `supportsMidConvoEffort` on OpenAI Responses / Codex Responses via positional `configuration_update` items, so mid-conversation effort changes on `gpt-6-astra` keep the prompt cache.

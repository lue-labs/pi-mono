---
"@valkyriweb/pi-coding-agent": patch
---

Keep the built-in Explore agent isolated with thinking disabled by default, and give it a child-scoped read-only tool allow-list that can activate installed deferred search tools without exposing generic deferred-tool discovery.

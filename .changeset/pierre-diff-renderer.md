---
"@valkyriweb/pi-coding-agent": patch
---

Add an experimental Pierre-backed TUI diff renderer for Edit previews behind `PI_TUI_DIFF_RENDERER=pierre`, while keeping the existing terminal renderer as the default fallback.

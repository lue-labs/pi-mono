---
"@valkyriweb/pi-coding-agent": patch
---

Prefer Claude-style `subagent_type`/`prompt` in the native Agent tool schema, batched task/chain entries, docs, and saved-chain JSON while preserving legacy `agent`/`task` aliases.

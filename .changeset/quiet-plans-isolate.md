---
"@valkyriweb/pi-coding-agent": patch
---

Run the built-in plan Agent without project instructions, preloaded skills, or append-system context by default, and preserve the parent's exact tool prefix for unrestricted general Agents while nested Agent execution remains depth-gated.

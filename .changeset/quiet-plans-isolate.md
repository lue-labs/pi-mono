---
"@valkyriweb/pi-coding-agent": patch
---

Run the built-in plan Agent without project instructions, preloaded skills, or append-system context by default, and let unrestricted general Agents reuse the parent's cache-affinity lane and exact provider-visible tool prefix while nested Agent execution remains depth-gated.

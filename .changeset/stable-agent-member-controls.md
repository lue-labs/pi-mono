---
"@valkyriweb/pi-coding-agent": patch
---

Separate Agent lifecycle from typed attention, add stable member-scoped controls and serialized single-child continuation, and reject unsupported chain or parallel input pauses explicitly.
